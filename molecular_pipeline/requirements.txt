# Runtime dependencies
SQLAlchemy>=2.0
requests>=2.28

# For PostgreSQL in production, also install a driver:
#   pip install "psycopg2-binary>=2.9"
# and set DATABASE_URL=postgresql+psycopg2://user:pass@host:5432/dbname

# For running the test suite:
#   pip install pytest
